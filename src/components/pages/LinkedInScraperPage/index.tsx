import { Wrapper, Form, Input } from './styled';

/**
 * Page with single form to scrape linkedIn profile link.
 */
export default () => (
  <Wrapper>
    <Form>
      <span>lala</span>
      <Input type='text' />
    </Form>
  </Wrapper>
);
