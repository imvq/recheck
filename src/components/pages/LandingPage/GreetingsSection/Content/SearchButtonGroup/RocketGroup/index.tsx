import * as styled from './styled';

/**
 * Rocket and its path (represented as a separate object).
 */
export default () => (
  <styled.Wrapper>
    <styled.Line />
    <styled.Rocket />
  </styled.Wrapper>
);
