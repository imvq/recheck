import ProfileMenuBadge from 'components/shared/ProfileMenuBadge';
import { Wrapper } from './styled';

/**
 * Cabinet wrapper.
 */
export default () => (
  <Wrapper>
    <ProfileMenuBadge />
  </Wrapper>
);
